FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package.json ./
RUN npm install

# Copy prisma and generate client
COPY prisma ./prisma
RUN npx prisma generate

# Copy source
COPY api.js ./

ENV NODE_ENV=production
ENV PORT=4000

EXPOSE 4000

CMD ["node", "api.js"] 